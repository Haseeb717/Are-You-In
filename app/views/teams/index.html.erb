<h1>Listing teams</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Sport</th>
      <th>City</th>
      <th>Gender</th>
      <th>Age group</th>
      <th>Age from</th>
      <th>Age to</th>
      <th>Public info</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><%= team.name %></td>
        <td><%= team.sport %></td>
        <td><%= team.city %></td>
        <td><%= team.gender %></td>
        <td><%= team.age_group %></td>
        <td><%= team.age_from %></td>
        <td><%= team.age_to %></td>
        <td><%= team.public_info %></td>
        <td><%= link_to 'Show', team %></td>
        <td><%= link_to 'Edit', edit_team_path(team) %></td>
        <td><%= link_to 'Destroy', team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Team', new_team_path %>
