<p> Hi <%= (@invitation.reciever.name || @invitation.reciever.first_name).titleize %> </p>
<p>
	You are being invited by <%= @invitation.sender.name.titleize %> to attend event <%= @invitation.event.title.titleize %> on <%= @invitation.event.date.strftime('%A %d %B, %Y') %> at <%= @invitation.event.team.city.titleize if @invitation.event.team.city %> at <%= @invitation.event.time.strftime('%I:%M %p') %>
</p>

<p>
	Are You In <br/>

	<%= link_to "Yes", invitation_response_url(:token => @invitation.token, :status => "in"), :class => "btn btn-primary" %>

	<%= link_to "Maybe", invitation_response_url(:token => @invitation.token, :status => "maybe"), :class => "btn btn-info" %>

	<%= link_to "No", invitation_response_url(:token => @invitation.token, :status => "out"), :class => "btn btn-danger" %>

</p>