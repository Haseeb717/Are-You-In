<div class="display-team-avatar">
	<img alt="<%= team_avatar.avatar.instance.avatar_file_name %>" src="<%= team_avatar.avatar.url %>">
	<br/>
	<% if team_avatar.user == current_user %>
		<div class="btn btn-danger remove-team-avatar" id="<%= team_avatar.id %>"> Remove file </div>
	<% end %>
</div>